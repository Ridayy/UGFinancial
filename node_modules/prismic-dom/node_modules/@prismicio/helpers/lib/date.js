"use strict";

module.exports = function (date) {
  if (!date) return null;
  var correctIso8601Date = date.length == 24 ? "".concat(date.substring(0, 22), ":").concat(date.substring(22, 24)) : date;
  return new Date(correctIso8601Date);
};